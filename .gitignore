# Godot
.godot/
*.translation
export/
# NOTE: export_presets.cfg IS committed — CI needs it to build
# releases (see .github/workflows/release.yml).

# Beads work-graph DB (per-machine, lives at /var/<project>/beads/)
.beads/

# Claude Code local settings (per-machine)
.claude/settings.local.json
